<?xml version="1.0" encoding="utf-8"?>
<!-- Recognition-mode marker. The fill is tinted at runtime; shape and size come from tokens.
     Mode is never carried by this dot alone — the status copy names it too. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@color/icon_on_surface" />
</shape>
