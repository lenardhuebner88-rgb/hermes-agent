# goal/sprint reliability progress

- Slice 1: gateway human timestamp weekday validation; AC: strip/reuse only when weekday matches parsed date in effective timezone; SHA: 936535c9b; GREEN: pytest tests/gateway/test_message_timestamps.py => 9 passed, 2 warnings; ruff check clean; git diff --check clean.
- Slice 2: Docker terminal JSON env shape validation; AC: Docker terminal JSON env vars fail fast with named ValueError when JSON type does not match expected shape; SHA: 7433722f; GREEN: pytest tests/tools/test_parse_env_var.py => 14 passed; ruff check clean; git diff --check clean.
- Slice 3: Empty terminal env vars fall back to defaults; AC: Empty string terminal environment variables are treated like unset values and fall back to configured defaults instead of raising parser errors; SHA: f25e769b; GREEN: pytest tests/tools/test_parse_env_var.py => 15 passed; ruff check clean; git diff --check clean.
- Slice 4: Normalize TERMINAL_ENV selector; AC: TERMINAL_ENV values with surrounding whitespace or mixed case are normalized before backend checks, so Docker/container settings are parsed consistently and env_type is canonical; SHA: 59218945; GREEN: pytest tests/tools/test_parse_env_var.py => 16 passed; ruff check clean; git diff --check clean.
- Slice 5: Trim boolean terminal env vars; AC: Boolean terminal environment variables accept surrounding whitespace before truthy checks, matching the existing case-insensitive behavior; SHA: 31434fd1; GREEN: pytest tests/tools/test_parse_env_var.py => 17 passed; ruff check clean; git diff --check clean; scripts/run-affected.sh => 9495 passed.
- Slice 6: Normalize empty boolean fallback defaults; AC: Empty boolean terminal environment variables fall back to defaults after applying the same whitespace/case normalization as explicit values; SHA: 978c4bbd; GREEN: pytest tests/tools/test_parse_env_var.py => 18 passed; ruff check clean; git diff --check clean.
- Slice 7: Empty TERMINAL_CWD falls back to backend default; AC: Empty TERMINAL_CWD values fall back to the backend default cwd instead of producing an empty cwd in terminal configuration; SHA: f0e5cbdd; GREEN: pytest tests/tools/test_parse_env_var.py => 19 passed; ruff check clean; git diff --check clean.
- Slice 8: Empty terminal image env vars fall back to defaults; AC: Empty terminal image environment variables fall back to configured default images instead of producing empty backend image names; SHA: 2cbdc054; GREEN: pytest tests/tools/test_parse_env_var.py => 20 passed; ruff check clean; git diff --check clean.
