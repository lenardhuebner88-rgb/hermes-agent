**BLOCK**

**hermes_cli/usage_consumption_readmodel.py:155** — `WHERE c.occurred_at >= ?` kombiniert mit dem `NOT EXISTS`-Fallback in Zeile 169 lässt jeden Run durchfallen, der Call-Zeilen mit `occurred_at IS NULL` hat. Beleg: `scripts/usage_reconcile.py:271-273` derselben Änderung schreibt „occurred_at is NULL for hermes_agent/hermes_aux (measured: 0 of 18k rows carry it)" und benutzt dort deshalb `COALESCE(f.last_call_at, f.captured_at)`. Gemessen gegen die Live-DB, Fenster 30 d: Origins im `run_usage_facts` = claude_code 99.104 / buzz_agent 6.358 / **hermes_agent 2.439** / qwen_cli 1.859 / grok_cli 1.016 / codex_cli 753 / **hermes_aux 400** / kimi_cli 136 — die Payload-Series liefert `[claude_code, codex_cli, buzz_agent, kimi_cli, qwen_cli, grok_cli]`, hermes_agent und hermes_aux fehlen vollständig (953.501.084 Tokens). Sie tauchen weder in `tokens` noch in `unpriced_tokens` noch im Nenner auf: `token_coverage` meldet 0,9489, während 8,8 % der Calls (20.888 von 120.791) und der gesamte Kanban-/Hermes-Runtime-Verbrauch weder gezählt noch als Lücke ausgewiesen sind.

**hermes_cli/usage_consumption_readmodel.py:385** — dasselbe Prädikat in `_per_run_rows`, also zusätzlich in `top_runs`, Verteilungen und den `chain_id`/`task_id`-Breakdowns. Beleg: 208 hermes_agent-Runs über 93 distinkte `task_id` liegen im Fenster und erscheinen in keinem Task-Bucket; live gemessen zeigt der `task_id`-Breakdown `('unknown', 98336)` und 205 echte Tasks — genau die Origins, die Task-Attribution tragen, sind ausgefiltert.

**hermes_cli/usage_facts_pricing.py:546** — `input_tokens - cache_read if 0 < cache_read <= input_tokens` ist rein zeilenweise und kennt den Provider nicht, obwohl `_estimate` ihn hat. Anthropics `input_tokens` enthält Cache-Reads nie. Live gemessen im 30-Tage-Fenster: 77 Zeilen mit `provider='anthropic'`, alle `origin='claude_code'` (also direkt aus dem Transkript-`usage`-Block), erfüllen die Bedingung — z. B. `claude-fable-5 input=24798 cache_read=22837`. Abgezogen werden 1.170.149 von 4.741.666 Anthropic-Input-Tokens = **24,7 % der Anthropic-Input-Basis**, still und ohne Note im `CostResult`.

**tests/hermes_cli/test_usage_facts_pricing.py:299** — `test_billable_input_untouched_for_anthropic_convention` behauptet im Namen und Docstring „Anthropic input excludes cache", prüft aber nur `input_tokens=1_000, cache_read_tokens=50_000`, also ausschließlich den Zweig, in dem die Bedingung ohnehin falsch ist. Der real auftretende Fall `cache_read <= input_tokens` bei Anthropic (77 Produktionszeilen, s. o.) wird von keinem Test berührt; der Test kann die Regression per Konstruktion nicht sehen.

**tests/hermes_cli/test_usage_consumption_readmodel.py:31** — sämtliche Fixture-Calls setzen `occurred_at` (`RECENT`/`OLD`, Zeilen 54 und 126). Keine einzige Fixture-Zeile hat `occurred_at IS NULL`, das sind in der Live-DB 22.991 von 140.887 Call-Zeilen. Die 12 grünen Tests belegen den Payload-Vertrag, nicht seine Vollständigkeit — sie sind der Grund, warum der Blocker oben unbemerkt blieb.

**plugins/kanban/dashboard/usage_facts_routes.py:113** — `threading.Thread(...).start()` auf Modulebene: jeder Import des Route-Moduls (auch CLI, pytest-Collection, jeder Worker, der das Kanban-Dashboard-Package importiert) öffnet im Hintergrund die Produktions-`usage_facts.db` und `kanban_db.kanban_db_path(board=_resolve_board(None))`. Fehler werden von `except Exception: pass` verschluckt, es gibt keinen Opt-out und keine Bindung an den App-Lifecycle — der Warmup gehört an ein FastAPI-Startup-Event, nicht an den Import.
EXIT=0
