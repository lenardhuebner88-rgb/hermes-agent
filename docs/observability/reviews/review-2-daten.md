## BLOCK

**hermes_cli/usage_consumption_readmodel.py:155 (+168–169)** — Runs, die Call-Zeilen mit `occurred_at IS NULL` haben, fallen aus **beiden** Abfragen: der Call-Pfad filtert `c.occurred_at >= ?` (NULL → false), der Run-Pfad greift nur bei `NOT EXISTS`-Runs. Gemessen im 30-d-Fenster gegen die Live-DB: **hermes_agent 2.433 Runs / 949 M Tokens, hermes_aux 400 / 4,1 M, claude_code 3.799 (pre-v6) / 423 M** — zusammen 1,38 Mrd Tokens verschwinden spurlos (nicht als `unpriced_tokens`, nicht als `unknown`). Beleg: `series keys: ['claude_code','codex_cli','buzz_agent','kimi_cli','qwen_cli','grok_cli']` — die gesamte Hermes-Runtime ($893 Äquivalent laut /tmp/reconcile-30d.json `run_usage_facts:hermes`) fehlt im Tab. usage-facts.md:39-40 behauptet ausdrücklich den Fallback `captured_at` als „einzige Zeit für hermes_agent/hermes_aux" — Code und Doku widersprechen sich.

**hermes_cli/usage_consumption_readmodel.py:1014 (+1026)** — Rate ohne passenden Nenner: der 7-d-Zähler filtert auf **Datumsstring** (`str(day) < since_iso[:10]`), umfasst also 8 Kalendertage, wird aber durch 7 geteilt; der Fenster-Nenner nutzt den exakten SQL-Zeitcutoff. Nachgerechnet, gleicher `now`: Trend meldet `equivalent_usd_per_day_7d = 694,88` / `ratio = 1,126×`, das exakte 7-Tage-Fenster derselben Funktion liefert `3832,07 / 7 = 547,44` → korrektes Verhältnis **0,887×**. Die KPI-Kachel (VerbrauchView.tsx:156) dreht damit das Vorzeichen der Aussage („Verbrauch steigt" statt sinkt).

**hermes_cli/usage_consumption_readmodel.py:678** — `kanban_eq = total_eq  # conservative` ist das Gegenteil von konservativ: der Fehl-Token-Anteil aus `task_runs` (Kanban-Scope, in+out ohne Cache, Korrelations-Abdeckung laut usage-facts.md Blind Spot 2 nur 16,5 %) wird auf das **gesamte** Fenster-Äquivalent inklusive codex_cli, buzz_agent, kimi/qwen und 96 k unkorrelierter claude_code-Runs hochgerechnet. Ergebnis live: `failed-outcome-runs current 5.312,73 / savings 2.656,37` — Rang 2 der Hebelliste, gleichrangig neben gerechneten Hebeln dargestellt (VerbrauchView.tsx:239-249), ohne dass `assumption` die Scope-Extrapolation nennt. `share_of_total` ist zusätzlich zirkulär (`_rate(share*total_eq, total_eq)` = share).

**hermes_cli/usage_consumption_readmodel.py:419 (+837)** — `_breakdown_from_run_rows` setzt `billing_mode=None`, also *unbekannt*; `_sums` addiert dann nichts und die Serie gibt trotzdem Zahlen aus. Live bei `breakdown=chain_id`: jede Zeile `metered_usd 0.0`, `subscription_saving_usd 0.0`, während dieselben Daten unter `breakdown=origin` $18.519,50 Ersparnis zeigen. „Unbekannt" ist hier still 0 geworden; `SeriesEntrySchema` (hooks/usageConsumption.ts:33-43) hat kein `metered_coverage`, das es aufdecken könnte. (Nebenbefund derselben Zeile: chain-Breakdown summiert $18.453,40 statt $18.518,37 — zwei Pfade, 0,35 % Differenz, unkommentiert.)

**docs/observability/usage-facts.md:71-86 vs. usage_consumption_readmodel.py:558-633** — der ausgelieferte Hebel `foreign-lane-cache-hit` steht in keiner Metrikdefinition, obwohl die Datei sich als „verbindliche Definition jeder ausgelieferten Verbrauchsmetrik" deklariert. Er ist zudem auf echten Daten tot: `lane_rows` liest nur `run_llm_calls`, dort existieren im Fenster ausschließlich die Origins `claude_code` und `buzz_agent` (gemessen) — die Bedingung `origin.endswith("_cli")` trifft nie zu, die Fremd-Lanes schreiben laut usage-ist-zustand.md:23-26 nur Run-Ebene.

**web/src/control/views/VerbrauchView.tsx:147** — `delta="metered $0,00 — das kostete der Wegfall des Abos"` ist ein Literal, nicht `totals.metered_usd`; sobald eine Gruppe `payg/metered/api_key` trägt, behauptet die Kachel weiterhin $0,00. `totals.metered_coverage` wird in der ganzen View nirgends gerendert — der unbekannt-abgerechnete Anteil ist im UI unsichtbar.

**web/src/control/views/VerbrauchView.tsx:296-300** — `meanCoverage` ist das **ungewichtete** Mittel der Tages-Coverage, beschriftet als „X % gedeckt" für die Kachel; ein Tag mit 10 Tokens zählt so viel wie ein Tag mit 10 Mrd. Nenner der Kachel wäre `Σ tokens`, nicht `Anzahl Tage`.

Nicht beanstandet (nachgerechnet, stimmt): billable_input-Regel zeilenweise identisch in SQL (L62-68) und `_billable_components` (usage_facts_pricing.py:543-549); Cache-Hit-Rate `cache_read/(billable_input+cache_read)` (L910-912) deckt beide Konventionen korrekt ab; TTL-Split-Regel verliert real 7.386 Tokens auf 99.896 Calls (< 0,01 ‰); `estimate_usage_cost` hält das `included`-$0 sauber von `estimate_equivalent_cost` getrennt (usage_facts_pricing.py:595, nur bei `result_status == "estimated"`); `task_runs.started_at` wird korrekt als Epoch gecastet (L667); Zod-Schema führt Geld/Raten als `number | "not applicable"` ohne `.catch(0)`.
EXIT=0
