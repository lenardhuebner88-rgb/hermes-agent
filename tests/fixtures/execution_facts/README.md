# Execution Facts fixtures

All fixtures are content-free. `system-observations.jsonl` exercises the
versioned CLI bridge for systemd and crontab milestones.
`subscription-fees.example.json` demonstrates explicit monthly USD fee input;
it contains no credentials and is never loaded implicitly.
